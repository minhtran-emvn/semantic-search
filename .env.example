# Backend configuration
# CORS_ORIGINS must be a JSON array string, not a comma-separated list.
CORS_ORIGINS=["https://semantic-search.emvn.co"]
AUDIO_DIR=/data/audio
EMBEDDINGS_DIR=/data/embeddings
CLAP_DEVICE=cpu
API_HOST=0.0.0.0
API_PORT=8000

# Frontend build-time configuration
VITE_API_BASE_URL=https://semantic-search.emvn.co
